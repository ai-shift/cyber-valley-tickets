import { AppContainer } from "@/shared/widgets/layout/AppContainer";
import { Router } from "../routes/Router";

export const App = () => {
  return (
    <AppContainer>
      <Router />
    </AppContainer>
  );
};
