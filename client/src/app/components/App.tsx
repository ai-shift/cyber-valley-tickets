import AppContainer from "@/shared/ui/AppContainer";
import { Router } from "../routes/Router";

const App = () => {
  return (
    <AppContainer>
      <Router />
    </AppContainer>
  );
};

export default App;
