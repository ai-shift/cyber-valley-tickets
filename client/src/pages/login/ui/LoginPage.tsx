import { useRefreshSlice } from "@/app/providers";
import { Navigate } from "react-router";

export const LoginPage: React.FC = () => {
  //   const { hasJWT } = useRefreshSlice();
  //   return hasJWT ? <div>Login</div> : <Navigate to="/" />;
  return <p>Joppa!</p>;
};
